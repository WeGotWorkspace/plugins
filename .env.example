# Release ZIP signing (RSA PEM). Optional locally; set as GitHub Actions secret for signed releases.
# WGW_RELEASE_SIGNING_PRIVATE_KEY=

# Git tag signing (SSH public key path). Required for pnpm release:*:publish.
# WGW_GIT_SIGNING_PUBLIC_KEY=$HOME/.ssh/id_ed25519.pub
